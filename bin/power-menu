#!/usr/bin/env bash
set -euo pipefail

config_home="${XDG_CONFIG_HOME:-$HOME/.config}"
layout="${config_home}/wlogout/layout"
style="${config_home}/wlogout/style.css"
exec wlogout --layout "${layout}" --css "${style}" --protocol layer-shell
