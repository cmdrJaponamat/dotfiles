# dotfiles Recovery

Project path: /home/japonamat/dotfiles
Description: основной репозиторий пользовательского окружения и переносимых конфигов
Status: active

## Purpose

- Хранить переносимые пользовательские конфиги и сценарии настройки окружения.
- Быть ключевым проектом для миграции окружения на другой ПК.
- Держать под git то, что определяет поведение UI, shell, редакторов и системных helper-скриптов.

## Current State

- Репозиторий отслеживается в `~/ai/projects.db`.
- Remote переведен на `github-auto` для automation push.
- В репозитории уже есть конфиги для `niri`, `alacritty`, `fish`, `nvim`, `rofi`, `hypr`, `mako`, `ranger`, `swayidle`, `swaylock`, `wlogout`, `zathura` и сопутствующих каталогов.
- В `system/pacman-hooks/` лежит переносимый шаблон hook для пометки package inventory как stale после `pacman`-транзакций.
- Live hook для `pacman` на текущей машине уже установлен вручную через `sudo`.
- Аппаратно-зависимые части пока еще не полностью отделены от переносимой базы, это остается задачей на будущее.

## Key Commands

- Проверить состояние репозитория:
  - `git -C ~/dotfiles status`
- Отправить изменения:
  - `git -C ~/dotfiles push`
- Установить переносимый `pacman` hook на новой машине:
  - `sudo ~/dotfiles/system/pacman-hooks/install-ai-package-inventory-hook.sh`
- Проверить инвентарь системы после изменений:
  - `ai-system-inventory status`
  - `ai-system-inventory refresh`
- Зафиксировать системное изменение и протухание inventory:
  - `ai-system-change "..." "..."`

## Next Steps

- Постепенно разделить переносимые настройки и machine-specific overlays.
- Уточнить manifest package groups после первого прогона bootstrap на чистой машине.
- Уточнить, какие каталоги из `dotfiles` являются каноническими, а какие устаревшим наследием прошлой схемы.
- Поддерживать recovery-файл в актуальном состоянии после заметных изменений в структуре репозитория и сценариях миграции.
- Довести адаптивность `waybar` по ширине и окончательно убрать противоречия вокруг `wleave`.

## 2026-08-28 UI Density And Session Menu Pass

- Добавлен рабочий план в `WORK_PLAN.md` с этапами и подзадачами.
- `wleave` убран из дефолтного install path:
  - больше не входит в `DOTBOOTSTRAP_DEFAULT_COMPONENTS`;
  - убран его пакетный путь как рекомендуемый активный сценарий.
- Зафиксировано, что активный session menu сейчас — `wlogout`.
- `ui-density` и `bootstrap.sh` теперь управляют не только размером шрифта `waybar`, но и текстовой плотностью длинных модулей:
  - `network`
  - `custom/playerctl`
- Для профилей `tiny/compact/normal/large/huge` заданы разные лимиты длины, чтобы большие UI-профили не выталкивали правые модули за пределы панели.
- `niri/waybar/config` переведён с `hypr/component/waybar/custom_modules/*` на собственные `niri/waybar/custom_modules/*` для:
  - `custom-kdeconnect`
  - `custom-cpugovernor`
  - `custom/playerctl`
- `niri/autostart.sh` очищен от автозапуска `Telegram`, потому что `Telegram` уже стартует через `~/.config/autostart/`.
- Текущее правило источника истины:
  - runtime для активной сессии идёт через `niri/*` и `bin/*`;
  - `hypr/*` пока сохраняется как legacy-конфиг и хранилище ассетов, в том числе обоев.
- Прямые runtime-ссылки `niri` на `hypr` дополнительно сокращены:
  - `niri/autostart.sh` теперь сначала ищет обои и `rgb` в `niri/*`;
  - `niri/scripts/wall-random` и `niri/scripts/wall-vim` сначала используют `niri/wallpapers`, а legacy `hypr/wallpapers` оставляют только как fallback;
  - в `niri/waybar/config` убран последний живой вызов `hypr/.../mediaplayer.py`.
- Введён канонический XDG autostart-слой в репозитории:
  - добавлен компонент `autostart -> ~/.config/autostart`;
  - override-файлы для `remmina-applet`, `slimbookbattery-autostart`, `blueman` и `org.fcitx.Fcitx5` теперь живут в `dotfiles/autostart/`;
  - это фиксирует policy без ручного дрейфа в live `~/.config/autostart`.
- `bootstrap.sh` получил post-install блок:
  - после `install` и `--dry-run install` печатает hardware-aware шаги завершения;
  - отдельно предупреждает про `SDDM`, `TLP`, `NetworkManager/Bluetooth`, touchscreen и forced `ui-density`;
  - это снижает шанс, что новая машина останется в полунастроенном состоянии после простого копирования конфигов.
- В manifest добавлены `optional flows`:
  - они явно отделяют ручные и спорные шаги от основной автоматической установки;
  - сейчас туда вынесены `AUR helper`, `slimbookbattery`, включение `SDDM`, `NetworkManager/Bluetooth`, `TLP` и ручной сценарий для `nvidia`.

## 2026-08-29 Waybar Density Hardening

- `ui-density` и `bootstrap.sh` скорректированы для больших профилей:
  - `large` больше не раздувает `network/playerctl` сильнее обычного;
  - `huge` для `waybar` намеренно ужат по `font-size` и текстовым лимитам, чтобы не ломать ноутбучную ширину.
- Исправлен regex-баг в rewrite-логике:
  - раньше `network` мог случайно получить `max-length` от `playerctl`;
  - теперь оба модуля меняются по своим JSON-блокам независимо.
- `media-player-status.py` теперь реально режет длинные заголовки на источнике, а не только полагается на `waybar max-length`.
- `niri/waybar/style.css` дополнительно ужат по padding/margin:
  - уменьшен воздух у правых модулей;
  - уменьшен расход ширины у `tray`, `language`, `touchscreen`, `power-draw`, workspace buttons.
- Live-окружение синхронизировано через `bootstrap --component local-bin --component niri` и затем `~/.local/bin/ui-density huge`.

## 2026-08-30 Waybar Power Draw States

- Для `custom/power-draw` добавлены реальные цветовые состояния через CSS-классы:
  - `low`
  - `medium`
  - `high`
  - `unknown`
  - `charging-state/full/not-charging`
- `power-draw.py` теперь:
  - возвращает `unknown`, если `power_now` недоступен;
  - возвращает скрытое charging-состояние с явным классом;
  - использует пороги `medium >= 9W`, `high >= 14W`.
- На текущей машине модуль отдал:
  - `text = PWR 16.4W 16.0W 15.9W`
  - `class = ["high", "discharging"]`
- Исправлена скрытая runtime-проблема:
  - `bin/relaunch-waybar` и live `~/.local/bin/relaunch-waybar` получили `+x`.
- Строка модуля упрощена:
  - вместо постоянного вывода трёх значений теперь показывается только `AVG <10m>`;
  - детальные числа, диапазон и sparkline-тренд вынесены в tooltip.
- По клику открывается отдельный отчёт `power-draw-report.py` в `alacritty` с:
  - instant/5m/10m/30m;
  - median;
  - range;
  - sparkline-трендом;
  - последними сэмплами.

## 2026-08-30 Wlogout PNG Rollback

- Отменён ошибочный перевод активного `wlogout`-стиля на SVG.
- Канонический активный путь для `wlogout` снова зафиксирован как перекрашенные стандартные PNG-иконки.
- SVG остаются в дереве только как неактивные legacy-ассеты и не используются runtime-стилем.
- `wlogout/style.css` при этом сохранил полезные косметические правки:
  - затемнение фона;
  - размер иконок;
  - радиусы;
  - padding/margin;
  - focus-state.
- Live `~/.config/wlogout/style.css` синхронизирован с репозиторием и совпадает с ним побайтно.

## 2026-08-26 Bootstrap Refresh

- Добавлен `bootstrap.sh` с manifest-driven установкой:
  - умеет читать совместимый manifest;
  - умеет брать локальный репозиторий или внешний git URL;
  - делает `check`, `list`, `install`;
  - перед копированием валидирует структуру и предупреждает о типичных portability-patterns.
- `install.sh` сохранен как совместимый wrapper.
- В переносимую базу синхронизированы две live-настройки:
  - `nvim/lua/plugins.lua`: отключен lazy checker;
  - `user-dirs.dirs`: добавлен `XDG_PROJECTS_DIR`.
- Осознанно не синхронизированы machine-specific/live-only расхождения:
  - `mimeapps.list` с `userapp-Thunderbird-*`;
  - `niri/config.kdl` с хоткеем на `~/.local/bin/ai-launcher`;
  - `niri/autostart.sh` со `slimbookbattery`.

## 2026-08-26 Portability Pass

- Добавлен переносимый `bin/`-слой, который bootstrap ставит в `~/.local/bin`:
  - `portable-brightness`
  - `portable-color-pick`
  - `optional-run`
- `niri` и `hypr` переведены с прямых вызовов `light`, `brightnessctl`, `color-pick` и `slimbookbattery` на переносимые wrappers.
- `swayidle` исправлен с чужого абсолютного пути `/home/obsy/...` на `$HOME`.
- `hypr/scripts/wall*` убраны с хардкоженных `/home/japonamat/...` в пользу `XDG_CONFIG_HOME` и `XDG_CACHE_HOME`.

## 2026-08-26 Hardware-Aware Bootstrap

- Bootstrap теперь определяет hardware tags и подбирает package groups под машину:
  - `laptop`, `battery`, `backlight`, `wifi`, `bluetooth`, `touchpad`, `internal_display`
  - `intel_gpu`, `amd_gpu`, `nvidia_gpu`
- Добавлены ручные overrides:
  - `--hardware-profile laptop|desktop|auto`
  - `--hardware-tag <tag>`
  - `--skip-hardware-tag <tag>`
- Manifest package groups теперь поддерживают hardware selectors, поэтому новый ноут получает только релевантные дополнительные пакеты вместо одной жесткой общей корзины.

## 2026-08-26 Hardware Probe Layer

- Добавлен `--probe-only` для отдельного вывода hardware summary без копирования и установки.
- Bootstrap теперь собирает:
  - hardware tags
  - hardware facts
  - probe notes
  - probe recommendations
- Сейчас probe особенно помогает перед миграцией на новый ноут:
  - подсказывает про hybrid graphics
  - напоминает про brightness wrappers
  - напоминает проверить включение wireless-сервисов после первого старта
  - подсказывает отдельно проверить touchscreen workflows, если будет найден touchscreen

## 2026-08-27 Package Presets

- Пакетный слой перестроен в пресеты полезности:
  - `minimal` — только минимально нужное для запуска пользовательской Wayland-сессии и hardware-specific userland
  - `core` — основной повседневный набор поверх `minimal`
  - `full` — расширенный набор с дополнительными приложениями
- Package groups теперь имеют явный уровень:
  - `minimal`
  - `core`
  - `full`
- Bootstrap получил `--package-preset minimal|core|full`.
- Preset-логика комбинируется с hardware selectors, поэтому новый ноут получает и нужный уровень пакетов, и только релевантные аппаратные группы.

## 2026-08-27 Gruvbox Baseline Pass

- Зафиксирована единая базовая тема системы:
  - GTK3/GTK4: `Gruvbox-Dark-BL`
  - Qt5/Qt6: `gruvbox.conf` + `Papirus-Dark`
  - Kvantum: добавлен в `dotfiles` с темой `Gruvbox-Dark-Brown`
- `rofi` shared color imports переведены с `onedark` на `gruvbox`.
- `waybar` и `mako` подогнаны под ту же палитру и более согласованные шрифты.
- `swaylock` получил более аккуратный gruvbox-конфиг вместо прежнего грубого варианта.
- Следующий шаг по UI: живой просмотр и доработка именно `swaylock` и `rofi`, если визуально захочется ещё меньше шума.

## 2026-08-27 Session Power Menu

- В качестве готового решения выбран `wlogout`, а не ещё один кастомный rofi-слой.
- Добавлены helper-скрипты:
  - `bin/power-menu`
  - `bin/session-logout`
- `wlogout/layout` исправлен под `niri`:
  - logout больше не использует `hyprctl`
  - logout идёт через `session-logout`
  - suspend сначала пытается поставить воспроизведение на паузу, затем уводит машину в сон
- `wlogout/style.css` приведён к более спокойному gruvbox-виду.
- В `niri/config.kdl` добавлен хоткей `Mod+Shift+P` для открытия power menu.
- Старый `Mod+Shift+P` для отключения мониторов перенесён на `Mod+Ctrl+P`.

## 2026-08-27 Wleave Migration

Статус: superseded

Примечание:

- Эта ветка эксперимента больше не является активным путём.
- Текущий активный session menu снова зафиксирован как `wlogout`.

- Основной session menu переведён на `wleave` с fallback на `wlogout`.
- Добавлены:
  - `wleave/layout.json`
  - `wleave/style.css`
  - `wleave/icons/*.svg`
- `bin/power-menu` теперь:
  - запускает `wleave`, если бинарь доступен;
  - иначе откатывается на `wlogout`.
- Для `wleave` использованы локальные SVG-иконки с `currentColor`, чтобы иконки реально подчинялись CSS-цветам и не повторяли проблему фиолетовых PNG.
- Пакет установки для Arch: `wleave-git` из AUR.
- Автоматическая установка из текущей headless-сессии упёрлась в `sudo` password prompt, поэтому бинарь может потребовать ручной `yay -S wleave-git` из интерактивного терминала.

## 2026-08-27 SDDM Gruvbox Theme

- Добавлен переносимый системный компонент `SDDM`:
  - `sddm/codex-gruvbox/` — собственная тема логина в палитре `gruvbox dark`
  - `sddm/10-theme.conf` — выбор этой темы через `/etc/sddm.conf.d/`
- `bootstrap.sh` теперь умеет копировать компоненты не только в `$HOME`, но и в абсолютные системные пути, если они указаны в manifest.
- В manifest добавлены:
  - компоненты `sddm-theme` и `sddm-config`
  - package group `display-manager` c `sddm` и `qt6-svg`
- Это закрывает разрыв между `SDDM` и уже приведёнными к `gruvbox` `swaylock`/`wlogout`, поэтому экран входа теперь тоже входит в переносимую сборку.
- Применение темы на текущей машине ещё не делалось автоматически, потому что это системная установка в `/usr/share/sddm/themes` и `/etc/sddm.conf.d`, то есть отдельный `sudo`-шаг.

## 2026-08-27 SilentSDDM Migration

- Самописная тема `codex-gruvbox` оставлена как запасной черновик, но основным вариантом для `SDDM` выбран готовый upstream `SilentSDDM`:
  - источник: `https://github.com/uiriansan/SilentSDDM`
  - активный preset: `configs/gruvbox.conf`
- В `dotfiles` добавлен theme payload `sddm/silent/` с нужными runtime-файлами темы.
- `sddm/10-theme.conf` теперь выбирает `Current=silent`.
- Добавлен отдельный системный конфиг `sddm/20-silent-general.conf` для:
  - `InputMethod=qtvirtualkeyboard`
  - `GreeterEnvironment=QML2_IMPORT_PATH=/usr/share/sddm/themes/silent/components/,QT_IM_MODULE=qtvirtualkeyboard`
- `manifest/default.manifest.sh` обновлён:
  - `sddm-theme` теперь ставит `SilentSDDM`
  - добавлен компонент `sddm-greeter`
  - `display-manager` расширен зависимостями `qt6-virtualkeyboard`, `qt6-multimedia-ffmpeg`, `qt6-imageformats`
- Dry-run установки отрабатывает, поэтому следующий шаг для живой системы — повторная установка `SDDM`-компонентов через bootstrap и визуальная проверка темы.
