# dotfiles Recovery

Project path: /home/japonamat/dotfiles
Description: основной репозиторий пользовательского окружения и переносимых конфигов
Status: active

## Purpose

- Хранить переносимые пользовательские конфиги и сценарии настройки окружения.
- Быть ключевым проектом для миграции окружения на другой ПК.
- Держать под git то, что определяет поведение UI, shell, редакторов и системных helper-скриптов.

## Current State

- Репозиторий отслеживается в `~/ai/projects.db`.
- Remote переведен на `github-auto` для automation push.
- В репозитории уже есть конфиги для `niri`, `alacritty`, `fish`, `nvim`, `rofi`, `hypr`, `mako`, `ranger`, `swayidle`, `swaylock`, `wlogout`, `zathura` и сопутствующих каталогов.
- В `system/pacman-hooks/` лежит переносимый шаблон hook для пометки package inventory как stale после `pacman`-транзакций.
- Live hook для `pacman` на текущей машине уже установлен вручную через `sudo`.
- Аппаратно-зависимые части пока еще не полностью отделены от переносимой базы, это остается задачей на будущее.

## Key Commands

- Проверить состояние репозитория:
  - `git -C ~/dotfiles status`
- Отправить изменения:
  - `git -C ~/dotfiles push`
- Установить переносимый `pacman` hook на новой машине:
  - `sudo ~/dotfiles/system/pacman-hooks/install-ai-package-inventory-hook.sh`
- Проверить инвентарь системы после изменений:
  - `ai-system-inventory status`
  - `ai-system-inventory refresh`
- Зафиксировать системное изменение и протухание inventory:
  - `ai-system-change "..." "..."`

## Next Steps

- Постепенно разделить переносимые настройки и machine-specific overlays.
- Уточнить manifest package groups после первого прогона bootstrap на чистой машине.
- Уточнить, какие каталоги из `dotfiles` являются каноническими, а какие устаревшим наследием прошлой схемы.
- Поддерживать recovery-файл в актуальном состоянии после заметных изменений в структуре репозитория и сценариях миграции.

## 2026-08-26 Bootstrap Refresh

- Добавлен `bootstrap.sh` с manifest-driven установкой:
  - умеет читать совместимый manifest;
  - умеет брать локальный репозиторий или внешний git URL;
  - делает `check`, `list`, `install`;
  - перед копированием валидирует структуру и предупреждает о типичных portability-patterns.
- `install.sh` сохранен как совместимый wrapper.
- В переносимую базу синхронизированы две live-настройки:
  - `nvim/lua/plugins.lua`: отключен lazy checker;
  - `user-dirs.dirs`: добавлен `XDG_PROJECTS_DIR`.
- Осознанно не синхронизированы machine-specific/live-only расхождения:
  - `mimeapps.list` с `userapp-Thunderbird-*`;
  - `niri/config.kdl` с хоткеем на `~/.local/bin/ai-launcher`;
  - `niri/autostart.sh` со `slimbookbattery`.

## 2026-08-26 Portability Pass

- Добавлен переносимый `bin/`-слой, который bootstrap ставит в `~/.local/bin`:
  - `portable-brightness`
  - `portable-color-pick`
  - `optional-run`
- `niri` и `hypr` переведены с прямых вызовов `light`, `brightnessctl`, `color-pick` и `slimbookbattery` на переносимые wrappers.
- `swayidle` исправлен с чужого абсолютного пути `/home/obsy/...` на `$HOME`.
- `hypr/scripts/wall*` убраны с хардкоженных `/home/japonamat/...` в пользу `XDG_CONFIG_HOME` и `XDG_CACHE_HOME`.

## 2026-08-26 Hardware-Aware Bootstrap

- Bootstrap теперь определяет hardware tags и подбирает package groups под машину:
  - `laptop`, `battery`, `backlight`, `wifi`, `bluetooth`, `touchpad`, `internal_display`
  - `intel_gpu`, `amd_gpu`, `nvidia_gpu`
- Добавлены ручные overrides:
  - `--hardware-profile laptop|desktop|auto`
  - `--hardware-tag <tag>`
  - `--skip-hardware-tag <tag>`
- Manifest package groups теперь поддерживают hardware selectors, поэтому новый ноут получает только релевантные дополнительные пакеты вместо одной жесткой общей корзины.

## 2026-08-26 Hardware Probe Layer

- Добавлен `--probe-only` для отдельного вывода hardware summary без копирования и установки.
- Bootstrap теперь собирает:
  - hardware tags
  - hardware facts
  - probe notes
  - probe recommendations
- Сейчас probe особенно помогает перед миграцией на новый ноут:
  - подсказывает про hybrid graphics
  - напоминает про brightness wrappers
  - напоминает проверить включение wireless-сервисов после первого старта
  - подсказывает отдельно проверить touchscreen workflows, если будет найден touchscreen

## 2026-08-27 Package Presets

- Пакетный слой перестроен в пресеты полезности:
  - `minimal` — только минимально нужное для запуска пользовательской Wayland-сессии и hardware-specific userland
  - `core` — основной повседневный набор поверх `minimal`
  - `full` — расширенный набор с дополнительными приложениями
- Package groups теперь имеют явный уровень:
  - `minimal`
  - `core`
  - `full`
- Bootstrap получил `--package-preset minimal|core|full`.
- Preset-логика комбинируется с hardware selectors, поэтому новый ноут получает и нужный уровень пакетов, и только релевантные аппаратные группы.

## 2026-08-27 Gruvbox Baseline Pass

- Зафиксирована единая базовая тема системы:
  - GTK3/GTK4: `Gruvbox-Dark-BL`
  - Qt5/Qt6: `gruvbox.conf` + `Papirus-Dark`
  - Kvantum: добавлен в `dotfiles` с темой `Gruvbox-Dark-Brown`
- `rofi` shared color imports переведены с `onedark` на `gruvbox`.
- `waybar` и `mako` подогнаны под ту же палитру и более согласованные шрифты.
- `swaylock` получил более аккуратный gruvbox-конфиг вместо прежнего грубого варианта.
- Следующий шаг по UI: живой просмотр и доработка именно `swaylock` и `rofi`, если визуально захочется ещё меньше шума.
